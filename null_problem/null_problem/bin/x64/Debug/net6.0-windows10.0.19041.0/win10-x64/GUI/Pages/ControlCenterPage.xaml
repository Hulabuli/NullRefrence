<Page
    x:Class="GUI.Pages.ControlCenterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:GUI.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:ControlCenterViewModel}"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <TextBlock Text="ControlCenterPage"/>
        <NumberBox x:ConnectionId='2' 
            x:Name="NumberBoxSpinButtonPlacementExample"
            Value="{Binding Session, Mode=TwoWay}"
            SpinButtonPlacementMode="Compact"
            SmallChange="1"
            />
        <Button Content="Switch Session" Command="{Binding SessionSwitch}"/>
    </StackPanel>
</Page>

