<UserControl
    x:Class="GUI.UserControls.ChannelBlockUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUI.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:usercontrols="using:GUI.ViewModels.UserControls" 
    xmlns:conv="using:GUI.Converters"
    d:DataContext="{d:DesignInstance usercontrols:ChannelBlockViewModel}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="Border">
            <Setter Property="Background" Value="DeepSkyBlue"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="CornerRadius" Value="7"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Padding" Value="4,0,4,0"/>
        </Style>

        <conv:IdConverter x:ConnectionId='2' x:Name="IdConverter"/>
        <conv:UnitConverter x:ConnectionId='3' x:Name="UnitConverter"/>
    </UserControl.Resources>

    <!--{Binding ChannelData.Active, Converter=StateConverter}-->
    
    <Border
        Opacity="1"
        Background="LightGray"
        Margin="0,5,0,0">
        <Grid ColumnSpacing="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <Border Grid.Column="0"
                    Grid.Row="0">
                <TextBlock 
                    Text="{Binding ChannelData.Id, Converter={StaticResource IdConverter}}"
                    VerticalAlignment="Center" 
                    Padding="6"
                    />
            </Border>

            <Border Grid.Column="1"
                    Grid.Row="0">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <TextBlock Text="{Binding ChannelData.Data}"/>
                    <TextBlock Text="{Binding ChannelData.Unit, Converter={StaticResource UnitConverter}}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>

